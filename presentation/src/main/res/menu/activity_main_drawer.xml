<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_camera"
            android:icon="@drawable/ic_menu_mail"
            android:title="@string/email" />
        <item
            android:id="@+id/nav_gallery"
            android:icon="@drawable/ic_menu_card"
            android:title="@string/credits" />
        <item
            android:id="@+id/nav_slideshow"
            android:icon="@drawable/ic_menu_web"
            android:title="@string/web" />
    </group>

    <item android:title="@string/options">
        <menu>
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_export"
                android:title="@string/send" />
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_menus_settings"
                android:title="@string/settings" />
            <item
                android:id="@+id/nav_delete"
                android:icon="@drawable/ic_menus_delete"
                android:title="@string/delete" />
        </menu>
    </item>

</menu>
